export type TFormData = {
  name: string;
  email: string;
  phone: string;
  plan: string;
  billing: string;
  addons: any[];
};

export type TError = {
  name: string;
  email: string;
  phone: string;
};
